<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE hibernate-configuration SYSTEM
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">
<hibernate-configuration>
    <session-factory>
        <!-- Database connection settings -->
        <property name="hibernate.connection.driver_class">org.postgresql.Driver</property>
        <property name="hibernate.connection.url">jdbc:postgresql://localhost:5432/lab_db</property>
        <property name="hibernate.connection.username">lab_user</property>
        <property name="hibernate.connection.password">lab_password</property>

        <!-- JDBC connection pool settings -->
        <property name="hibernate.connection.pool_size">10</property>
        <property name="hibernate.connection.autocommit">false</property>

        <!-- SQL dialect -->
        <property name="hibernate.dialect">org.hibernate.dialect.PostgreSQLDialect</property>

        <!-- Echo all executed SQL to stdout -->
        <property name="hibernate.show_sql">true</property>
        <property name="hibernate.format_sql">true</property>

        <!-- Drop and re-create the database schema on startup -->
        <property name="hibernate.hbm2ddl.auto">validate</property>

        <!-- Names the annotated entity class -->
        <mapping class="edu.sfu.entity.Author"/>
        <mapping class="edu.sfu.entity.Book"/>
        <mapping class="edu.sfu.entity.User"/>
        <mapping class="edu.sfu.entity.Genre"/>
        <mapping class="edu.sfu.entity.BookLoan"/>
    </session-factory>
</hibernate-configuration>